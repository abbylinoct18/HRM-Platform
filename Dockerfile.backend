# 使用輕量級 Python 基礎映像
FROM python:3.11-slim

# 設定工作目錄
WORKDIR /app

# 安裝依賴項
# 注意: requirements.txt 中的 uvicorn\[standard\] 必須正確安裝
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 複製應用程式碼
COPY main.py .

# 暴露 FastAPI 運行的埠號
EXPOSE 8000

# 運行應用程式的指令
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]